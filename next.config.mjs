// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // No `output: "export"` here
  experimental: {
    // appDir is on by default in Next 14, so don't put it here
  },
};

export default nextConfig;
